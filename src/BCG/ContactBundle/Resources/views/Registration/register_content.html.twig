{% trans_default_domain 'FOSUserBundle' %}

<div class="col-md-6 col-md-offset-3 top-space-md">
	<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">

		{{ form_start(form) }}
		{{ form_widget(form) }}
		{{ form_end(form) }}
	    
	    <input class="btn btn-primary btn-block top-space-md" type="submit" value="{{ 'registration.submit'|trans }}" />
	</form>
</div>